{
  "name": "codeshot-api",
  "version": "1.0.0",
  "description": "HTTP API server for CodeShot — renders beautiful code screenshots via curl",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
